#include "main.h"
/**
 * main - check the code
 *print string ff new line
 * Return: Always 0.
 */
int main(void)
{
    char *str;

    str = "I do not fear computers. I fear the lack of them - Isaac Asimov";
    puts(str);
    return (0);
}
